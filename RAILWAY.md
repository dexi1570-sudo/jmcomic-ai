# Railway 部署

仓库根目录的 railway.toml 指定 Docker 构建与 HTTP MCP 启动命令，修复自动检测无法找到启动命令的问题。Docker 镜像使用 Python 3.12，并安装当前仓库代码。

1. Railway 服务应连接本仓库的 master 分支，Root Directory 留空。
2. 部署最新提交。如果此前设置了自定义构建或启动命令，请清除旧覆盖值；以本仓库配置为准。
3. 查看构建和部署日志，确认服务监听 Railway 提供的 PORT。
4. 当前阶段不要生成公网域名：原版服务尚未配置连接鉴权。

## 当前限制

所有现有查询、登录、收藏和下载工具均保留。此配置仅解决构建与启动；未新增鉴权、后台任务或文件下载链接。原版下载结果仍返回服务器上的文件路径。

在依赖下载或登录状态前，应挂载持久化磁盘，并配置 JM_OPTION_PATH、JM_LOG_PATH、JM_TASK_LOG_DIR，以及 option.yml 中的下载目录，使文件保存在挂载目录内。不要提交账号密码、Cookie 或密钥。

HTTP MCP 的默认接口路径为 /mcp。请先完成连接鉴权，再公开服务和接入客户端。
